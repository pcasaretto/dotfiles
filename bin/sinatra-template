#!/bin/sh
name=$1
mkdir $name
cd $name

bundle init

echo "gem 'sinatra'" >> Gemfile

cat > app.rb << EOF
require 'sinatra'

get '/' do
  "Hello World!"
end
EOF

cat > config.ru << EOF
require './app'
run Sinatra::Application
EOF
